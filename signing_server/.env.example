# Signing server — template only
# Usage: cp .env.example .env  then set your mnemonic (offline)
# Must be the SAME seed as ACCOUNT_XPUB on the payment server.

MASTER_MNEMONIC_FOR_SIGNING="word1 word2 word3 ... word12_or_24"
