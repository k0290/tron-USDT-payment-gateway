
```bash
  **密钥生成 (重要：在离线设备上操作)**
    a. 运行 `python key_management/generate_seed.py` 生成你的助记词。**立即将助记词安全备份！**
    b. 将生成的助记词填入 `key_management/derive_xpub.py` 文件中。
    c. 运行 `python key_management/derive_xpub.py` 获取你的账户 xpub。
```